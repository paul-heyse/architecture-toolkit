# Default profile boundary

Implement generic type and relationship registries here. The current minimal validator
checks identity/endpoint structure, not registry semantics. Define permitted endpoints,
canonical direction/inverse labels, cardinality and traversal policy before claiming
architecture validation. Consumer-specific vocabularies belong in separately supplied
profiles and private consumer model repositories, never in the reusable core.
