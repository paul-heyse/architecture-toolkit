"""Several toolkit layers together: publication, storage and validation in one protocol.

Distinct from `tests/qualification/`, which asks whether a *library* behaves as the contract
assumes. These ask whether the toolkit's own protocol holds when the layers are wired together.
"""
